name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: setup-java
        uses: olafurpg/setup-scala@v1
      - uses: actions/checkout@v1
      - name: Build and test
        run: ./gradlew build test
